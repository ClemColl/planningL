<style>
.check_boxes {
  display: block;
}
.checkbox {
  display: inline-block;
  width:100px;
}
</style>
<%= simple_form_for(@equipe) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :re, label: "Responsable d'équipe" %>
    <%= f.association :personnes, as: :check_boxes, :label_method => :name, :value_method => :id %>
  </div>
  <% if action_name == "edit" %>
    <%= link_to 'Ajouter une personne', new_equipe_personne_path(equipe_id: @equipe), class: "btn btn-default" %>
  <% end %>

  <div class="form-actions">
    <%= f.button :submit, "Valider" %>
    <%= link_to 'Retour', equipes_path, class: "btn btn-default" %>
  </div>
<% end %>

