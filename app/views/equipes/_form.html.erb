<style>
.check_boxes {
  display: block;
}
.checkbox {
  display: inline-block;
  width:250px;
}
</style>
<%= simple_form_for(@equipe) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :re, label: "Responsable d'équipe" %>
    <%= f.association :personnes, collection: Personne.all.order(:name), as: :check_boxes, :label_method => :name, :value_method => :id %>
  </div>


  <div class="form-actions">

    <%= f.button :submit, "Valider" %>
    <% if action_name == "edit" %>
      <%= link_to 'Ajouter une personne', new_equipe_personne_path(equipe_id: @equipe), class: "btn btn-info" %>
      <%= link_to 'Modifier la liste de personnes', equipe_personnes_path(@equipe), class: 'btn btn-default' %>
    <% end %>
    <%= link_to 'Retour', equipes_path, class: "btn btn-default" %>
  </div>
<% end %>

