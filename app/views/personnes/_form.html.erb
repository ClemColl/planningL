<%= simple_form_for(@personne) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Nom" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
